# tabi

A toy project that addresses the annoying fact that (meta) build system are usually
not written in the same language as the code they build.

Tabi (a type of Japanese sock) is a build system written in C that uses C to 
desribe build rules. The only dependency is a C99 compiler and ninja to execute
the generated build files.

